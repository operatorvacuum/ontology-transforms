[project]
name = "operator-ontology"
version = "0.1.0"
description = "Transform language into operators, complements, assumptions, and implementations."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
test = [
  "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
