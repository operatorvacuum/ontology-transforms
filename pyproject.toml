[project]
name = "semantic-factorization-engine"
version = "0.2.0"
description = "Compile language into provenance-bearing candidate relational graphs."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
test = [
  "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
