# Next

## Current checkpoint

- Stable implementation checkpoint: `90f924e` (`Complete guarded recomposition milestone`).
- Seven fixture files exercise belonging, harmony, Good Engineers, Healthy
  Relationships, mentor guidance, respect, and Good Parents.
- Thirty-five direct regression functions passed before this documentation pass.
- The compiler exposes parsed/resolved/factorized/validated states, three
  operation-relative projections, depth-aware recursive rendering, and loss ledgers.
- Guarded recomposition supports `ACCEPT`, `REJECT`, and
  `RETAIN_HIGH_DIMENSIONAL`, including immutable operation-local branch selection.
- Current graph-emitted relations are documented in `AI_CONTEXT.md`; no ontology
  relation or runtime behavior changed in this documentation pass.
- At generation time the implementation checkpoint was clean. This handoff packet
  and the README pointer are intentionally uncommitted pending review.
- The README body still contains historical three-fixture wording and examples;
  the seven files in `examples/fixtures/` and this packet describe current scope.

## Current next task

Review and commit this documentation-only handoff checkpoint. After that, the
next likely engineering task is broader adversarial fixture and recomposition
coverage using the existing IR. Do not redesign the IR or add relations unless a
fixture demonstrates a distinction that cannot be represented cleanly.

## Deferred ideas

- Interaction/relational coordinates: mirroring, tone, gaze, clock-capture family.
- Broader fixture coverage.
- Downstream A/B test: raw prompt versus compiled semantic IR.
- Temporal trajectory and prediction engine later.
- Optional local-model prompt expansion/recomposition later.

## Open semantic questions

- `context` is stored but does not yet affect compilation; its first operational
  use must be fixture-driven.
- Qualifier compatibility is currently exact equality; no subsumption policy has
  been justified.
- Human recomposition rendering may later distinguish primary blockers from
  supporting diagnostics and present `RETAIN_HIGH_DIMENSIONAL` more directly in
  terms of preserved dimensions.
- Good Engineers currently has recursive evaluator expansion but no recursive
  engineer-configuration fragment; this asymmetry is current behavior, not an
  implicit roadmap commitment.
- `model_hypothesis`, `treated_as_equivalent_to`, and `distinct_from` are supported
  or recognized but are not emitted by current fixtures.
